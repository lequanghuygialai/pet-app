spring:
    application:
        name: petshop-api
    datasource:
      url: jdbc:mysql://localhost:3306/petshop
      username: root
      password: root
    jpa:
      database-platform: org.hibernate.dialect.MySQLDialect
      generate-ddl: true
      show-sql: true
      hibernate:
        ddl-auto: update
